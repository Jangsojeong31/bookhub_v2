name: deploy-backend workflow

on:
  push:
    paths: 
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
      
jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        working-directory: backend
      
      - name: Build with Gradle
        run: ./gradlew clean build -x test
        working-directory: backend
      
      - name: Copy file to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          source: "backend/build/libs/bookhub_back-0.0.1-SNAPSHOT.jar"
          target: "~/bookhub-deploy-backend"
      
      - name: Deploy to EC2 (run remote command)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            pkill -f 'java -jar' || true
            cd ~/bookhub-deploy-backend
            nohup java -jar bookhub_back-0.0.1-SNAPSHOT.jar > log.txt 2>&1 &
            sleep 5
